*English ->* It diversified the possible causes of conflicts between the versions of git and branches, since when working in collaborative there may be syntax errors or that we call the variables differently.
*Spanish ->*Se diversificó las posibles causas de conflictos entre las versiones de git y ramas, puesto que cuando se trabaja en colaborativo puede que hayan erroes de sintaxis o que llamemos diferente las variables.